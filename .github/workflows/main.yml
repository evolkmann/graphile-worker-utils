name: main

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: 'Install Dependencies'
        run: yarn
      - name: 'Build'
        run: yarn build
